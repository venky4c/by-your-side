import React from 'react'

function Button({children}) {
    //const { label } = props
    return (
        <div>
         <button>{children}</button>   
        </div>
    )
}

export default Button
